# Optional: only needed if you want favorites/progress/khatmah plans to sync
# across devices. Without these, the app works fully offline using
# localStorage/IndexedDB on this device only.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
